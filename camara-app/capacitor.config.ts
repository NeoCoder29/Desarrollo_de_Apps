import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.camaraapp',
  appName: 'Camara App',
  webDir: 'dist/camara-app/browser'
};

export default config;
